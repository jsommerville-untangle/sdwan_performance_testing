config interface 'loopback'
        option ifname 'lo'
        option proto 'static'
        option ipaddr '127.0.0.1'
        option netmask '255.0.0.0'

config globals 'globals'
        option ula_prefix 'fdf1:ab86:f5ab::/48'

config interface 'WAN04'
        option ifname 'eth1'
        option proto 'dhcp'

config interface 'WAN06'
        option ifname 'eth1'
        option proto 'dhcpv6'

config interface 'WAN14'
        option ifname 'eth2'
        option proto 'dhcp'

config interface 'WAN16'
        option ifname 'eth2'
        option proto 'dhcpv6'

config interface 'LAN4'
        option ifname 'eth3'
        option proto 'static'
        option ipaddr '192.168.43.1'
        option netmask '255.255.255.0'

config interface 'LAN6'
        option ifname 'eth3'
        option proto 'static'
        option ip6assign '64'
        option ip6hint '1234'